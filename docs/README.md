# docs/

Images referenced by the repository's README. Each one is described, at the
place it belongs, in an HTML comment inside `README.md` — search for `BANNER`,
`SCREENSHOT` or `DIAGRAM` there for the size, the content and the palette.

Expected files:

| File | Size | Shows |
|---|---|---|
| `banner.png` | 1280×420 | The mark, the wordmark, and a sector map that is mostly red with a scattering of green |
| `screenshot-scan.png` | ~1240×900 | The window during the verify phase, around 60–70% |
| `write-order.png` | ~880×300 | Why the write pass runs back to front |
| `layouts.png` | ~880×260 | Fenced versus spliced, on the same card |

Only the banner and the screenshot are worth doing first; the two diagrams
explain ideas the text already carries.

Palette, so the images sit with the interface rather than beside it:

| | |
|---|---|
| Background | `#0b1018` |
| Panel | `#141b26` |
| Approved | `#34d399` |
| Defective | `#f43f5e` |
| Contacts / gold | `#d8b45e` |
| Accent | `#38bdf8` |
| Dim text | `#a7b5c6` |
